#include "stdafx.h"
#include "Spikes.h"
#include "Jelly.h"

void Spikes::SpawnJelly(std::vector<GameObject*>& jellyList) {
    //sf::Vector2f start = this->GetPosition();
    //for (int i = 0; i < 5; ++i) {
    //    sf::Vector2f jellyPos = start + sf::Vector2f(i * 20.f, -30.f); // 선형으로 위에 배치
    //    jellyList.push_back(new Jelly(jellyPos));
    //}
}